server {
    listen 80;

    location /images {
        alias /var/www/yimclub/images;
    }

    location /static {
        alias /var/www/yimclub/static;
    }

    location / {
        proxy_pass http://localhost:2000;
    }
}
